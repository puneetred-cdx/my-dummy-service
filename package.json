{
  "name": "my-dummy-service",
  "version": "1.0.0",
  "description": "A dummy service to reproduce scanner issues.",
  "main": "build/app.js",
  "scripts": {
    "clean": "rm -rf build",
    "build": "npm run clean && npx tsc && cp -R src/templates build/templates",
    "start": "node build/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "ejs": "^3.1.6"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^16.11.7",
    "@types/ejs": "^3.1.0",
    "typescript": "^4.4.4",
    "ts-node": "^10.4.0"
  }
}